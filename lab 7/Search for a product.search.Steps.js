// searchSteps.js

const { Given, When, Then } = require('@cucumber/cucumber');

Given('I am on the homepage', async () => {
  // Navigate to the homepage
});

When('I search for a product', async () => {
  // Perform a search for a product
});

Then('I should see search results related to the product', async () => {
  // Verify that search results related to the product are displayed
});
