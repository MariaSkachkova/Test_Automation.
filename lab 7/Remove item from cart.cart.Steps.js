// cartSteps.js

const { Given, When, Then } = require('@cucumber/cucumber');

Given('I have items in my shopping cart', async () => {
  // Add items to the shopping cart
});

When('I remove an item from the cart', async () => {
  // Remove an item from the shopping cart
});

Then('the item should be removed from my shopping cart', async () => {
  // Verify that the item is removed from the shopping cart
});
